#include <stdio.h>

int main()
{
  int i = 2;

    while (i <= 10) {
        
  printf("%d\n", i);
  
  i= i +2;
    }
    
    int numero;
    printf("digite um numero:");
    scanf("%d", &numero);
    
    while (numero < 0){
        printf("por favor digite um número inteiro:");
        scanf("%d", &numero);
            }
            
             printf("Obrigada!");
        
    return 0;
}
